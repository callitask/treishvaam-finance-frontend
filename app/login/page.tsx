"use client";
/**
 * AI-CONTEXT:
 * Purpose: Next.js wrapper for the legacy CRA Login Page.
 *
 * IMMUTABLE CHANGE HISTORY (DO NOT DELETE):
 * - ADDED: App Router migration wrapper.
 */
import LoginPage from '../../src/pages/LoginPage';

export default function Page() {
    return <LoginPage />;
}
