# Allow all crawlers
User-agent: *

# Allow access to all content by default
Allow: /

# Disallow crawlers from indexing API, admin, and internal search paths
Disallow: /api/
Disallow: /dashboard/
Disallow: /?q=*

# Sitemap Index
Sitemap: https://treishfin.treishvaamgroup.com/sitemap.xml